
dubbo.address	  = ${dubbo_address}
dubbo.application	  = ${dubbo_application}

component.scan.package			  = ${component_scan_package}

authority.web.logging.root  			= ${authority_web_logging_root}
authority.web.logging.name  			= ${authority_web_logging_name}
authority.logging.level 			= ${authority_logging_level}

authority.web.freemarker.dir 	= ${authority_web_freemarker_dir}
authority.web.upload.maxsize		= ${authority_web_upload_maxsize}
authority.web.upload.errorpage	= ${authority_web_upload_errorpage}
authority.web.view.template.suffix =${authority_web_view_template_suffix}
